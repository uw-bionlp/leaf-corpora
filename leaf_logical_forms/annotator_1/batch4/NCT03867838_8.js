'EXC'

'-  have a condition ( other than stroke ) affecting sensorimotor function'

'-  have a cond() ( except() cond("stroke") ) change() cond("sensorimotor function")'

cond("sensorimotor function")
    .change()
    .caused_by(
        cond()
            .except(
                cond("stroke")
            )
    )