'EXC'

'-  History of H .  pylori eradication'

'-  eq(temporal_per(PAST)) of org("H .  pylori") proc()'

proc()
    .temporality(
        eq(temporal_per(PAST))
    )
    .for(
        org("H .  pylori")
    )