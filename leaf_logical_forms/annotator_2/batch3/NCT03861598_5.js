'EXC'

'-  Current use of another beta blocker that cannot be switched to carvedilol at the onset of the trial .'

'-  eq(temporal_per(PRESENT)) use of another drug("beta blocker") that cannot be switched to carvedilol at the onset of the trial .'

drug("beta blocker")
    .temporality(
        eq(temporal_per(PRESENT))
    )