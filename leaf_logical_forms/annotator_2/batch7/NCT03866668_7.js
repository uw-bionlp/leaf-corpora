'INC'

'-  no planned changes in psychosocial interventions during the open - label trial .'

'-  neg() eq(temporal_per(FUTURE)) change() in proc("psychosocial interventions") during the open - label trial .'

neg(
    proc("psychosocial interventions")
        .temporality(
            eq(temporal_per(FUTURE))
        )
        .change()
)