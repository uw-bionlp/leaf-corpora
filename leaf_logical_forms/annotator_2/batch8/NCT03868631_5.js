'EXC'

'-  participated in structured weight training during the previous 12 month'

'-  participated in mod("structured") social_habit("weight training") during the eq(temporal_per(PAST), val("12"), temporal_unit(MONTH))'

social_habit("weight training")
    .mod("structured")
    .temporality(
        eq(temporal_per(PAST), val("12"), temporal_unit(MONTH))
    )