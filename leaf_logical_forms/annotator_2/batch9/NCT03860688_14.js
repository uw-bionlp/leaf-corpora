'EXC'

'11.  Any changes to medication in the preceding month'

'11.  Any change() to drug() in the eq(temporal_per(PAST), temporal_unit(MONTH))'

drug()
    .change()
    .temporality(
        eq(temporal_per(PAST), temporal_unit(MONTH))
    )
    