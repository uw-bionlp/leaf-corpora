'INC'

'5.  Currently use other fertility monitoring tools ( this allows us to compare Proov - Ovulation Double Check test results with other fertility tracking methods )'

'5.  eq(temporal_per(PRESENT)) use other proc("fertility monitoring tools") ( this allows us to compare Proov - Ovulation Double Check test results with other fertility tracking methods )'

proc("fertility monitoring tools")
    .temporality(
        eq(temporal_per(PRESENT))
    )
