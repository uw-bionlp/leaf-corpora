'EXC'

'-  current recreational drug use for all subjects'

'-  eq(temporal_per(PRESENT)) cond("recreational drug use") for all subjects'

cond("recreational drug use")
    .temporality(
        eq(temporal_per(PRESENT))
    )