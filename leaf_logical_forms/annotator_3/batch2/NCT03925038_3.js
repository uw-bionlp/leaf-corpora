'INC'

'-  Already established and receiving care in community psychiatry at the Johns Hopkins Bayview Medical Center or at Johns Hopkins Hospital .'

'-  Already established and eq(temporal_per(PRESENT)) enc() in community prov("psychiatry") at the loc(hosp("Johns Hopkins Bayview Medical Center")) or at loc(hosp("Johns Hopkins Hospital")) .'

enc()
    .temporality(
        eq(temporal_per(PRESENT))
    )
    .prov("psychiatry")
    .loc(hosp("Johns Hopkins Bayview Medical Center"))
    .loc(hosp("Johns Hopkins Hospital"))