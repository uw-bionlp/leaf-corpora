'INC'

'-  no history of periodontal surgery .'

'-  neg() eq(temporal_per(PAST)) of proc("periodontal surgery") .'

neg(
    proc("periodontal surgery")
        .temporality(
            eq(temporal_per(PAST))
        )
)
