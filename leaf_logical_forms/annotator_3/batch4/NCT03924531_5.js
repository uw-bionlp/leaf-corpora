'EXC'

'-  On chemotherapy for cancer treatment'

'-  eq(temporal_per(PRESENT)) proc("chemotherapy") for proc("cancer treatment")'

proc("chemotherapy")
    .temporality(
        eq(temporal_per(PRESENT))
    )
    .equiv(
        proc("cancer treatment")
    )
