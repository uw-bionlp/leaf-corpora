'EXC'

'-  History of congenital maxillofacial defect ,'

'-  eq(temporal_per(PAST)) of mod("congenital") cond("maxillofacial defect") ,'

cond("maxillofacial defect")
    .num_filter(
        eq(temporal_per(PAST))
    )
    .mod("congenital")