'INC'

'-  immigrant ( not born in continental U . S . )'

'-  obs("immigrant") ( neg() birth() in continental loc(res("U . S .")) )'

obs("immigrant")
    .equiv(
        neg(
            birth()
                .loc(res("U . S ."))
        )
    )