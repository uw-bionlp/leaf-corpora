'EXC'

'-  Presence of depressive symptoms that could influence cognitive function'

'-  Presence of cond("depressive") possible() that could change() cond("cognitive function")'

cond("cognitive function")
    .change()
    .caused_by(
        cond("depressive")
            .possible()
    )