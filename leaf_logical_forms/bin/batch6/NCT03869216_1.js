'INC'

'-  Receiving care at the clinical sites'

'-  eq(temporal_per(PRESENT)) proc() at the loc(hosp("clinical sites"))'

proc()
    .temporality(
        eq(temporal_per(PRESENT))
    )
    .loc(hosp("clinical sites"))