'EXC'

'-  Presence of uncontrolled diabetes mellitus ,'

'-  eq(temporal_per(PRESENT)) of change() cond("diabetes mellitus") ,'

cond("diabetes mellitus")
    .temporality(
        eq(temporal_per(PRESENT))
    )
    .change()
