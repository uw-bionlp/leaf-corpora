'INC'

'-  ICSI cases using PICSI dishes for sperm selection'

'-  proc("ICSI") cases eq(temporal_per(PRESENT)) proc("PICSI dishes for sperm selection")'

proc("ICSI")
    .for(
        proc("PICSI dishes for sperm selection")
            .temporality(
                eq(temporal_per(PRESENT))
            )
    )